import type { Topic } from '../../../../types';

export const arrayOfPointers: Topic = {
  id: 'u2-t5',
  unitId: 'unit-2',
  title: 'Array of Pointers',
  slug: 'array-of-pointers',
  description: 'In C, an array can hold any data type — integers, floats, structs, and even pointers. An array of pointers is exactly what it sounds like: a contiguous block of memory where every element is a memory address pointing somewhere else. This simple concept provides the foundation for solving one of C\'s biggest limitations: rigid array sizes. By creating an array of pointers, we can build "jagged arrays" where each row has a completely different length. This is how C manages text efficiently, and it is how the operating system passes command-line arguments to your `main` function via the famous `argv` array.',
  difficulty: 'advanced',
  prerequisites: ['u2-t4'],
  estimatedMinutes: 50,
  subtopics: [
    {
      id: 'u2-t5-s1',
      title: 'Declaring and Using Arrays of Pointers',
      slug: 'array-of-pointers-basics',
      description: 'Think of an array of pointers as a directory or a table of contents. The array itself does not contain the actual data (like the text of a book); it only contains the addresses (page numbers) where the data can be found. This separation of "the list" from "the data" gives us incredible flexibility.',
      keyPoints: [
        'The declaration syntax combines array brackets and pointer stars: `type *arr[size];`. For example, `int *arr[3];` declares an array named `arr` containing 3 elements. Each of those 3 elements is a pointer to an `int`.',
        'Just like any other array, an array of pointers contains garbage values when first declared. You must initialize each element to hold a valid memory address (e.g., `arr[0] = &someVar;`) or `NULL`.',
        'To access the actual data, you must perform two steps. First, index into the array to get the pointer: `arr[i]`. Second, dereference that pointer to get the value: `*arr[i]`.',
        'The most common use case by far is an array of strings. Since a string in C is just a `char *` (a pointer to the first character), an array of strings is naturally a `char *arr[size]`.',
        'This technique creates "jagged arrays." In a standard 2D array (`char grid[3][100]`), every row must be 100 bytes long, wasting massive amounts of memory if you only store "Hi". With an array of pointers (`char *grid[3]`), row 1 can point to a 3-byte string, and row 2 can point to a 100-byte string.',
      ],
      codeExamples: [
        {
          id: 'u2-t5-s1-ex1',
          title: 'Array of Integer Pointers',
          code: '#include <stdio.h>\n\nint main(void) {\n    int a = 10, b = 20, c = 30;\n    /* An array of 3 pointers to int */\n    int *ptrs[3]; \n\n    ptrs[0] = &a;\n    ptrs[1] = &b;\n    ptrs[2] = &c;\n\n    /* Printing the values by dereferencing the pointers in the array */\n    for (int i = 0; i < 3; i++) {\n        printf("Value %d: %d\\n", i, *ptrs[i]);\n    }\n    return 0;\n}',
          language: 'c',
          explanation: 'This code isolates the mechanics of an array of pointers. The array `ptrs` occupies 24 bytes in memory (3 pointers, 8 bytes each). It does not contain the numbers 10, 20, or 30. It only contains the three hex memory addresses where `a`, `b`, and `c` live. By indexing the array and then dereferencing the result, we remotely access the original integers.',
          expectedOutput: 'Value 0: 10\nValue 1: 20\nValue 2: 30',
          lineBreakdown: [
            { lineNumber: 6, code: '    int *ptrs[3];', explanation: 'The brackets `[3]` bind to `ptrs` first, meaning "an array of 3". The `int *` tells us what goes inside: pointers to integers.' },
            { lineNumber: 8, code: '    ptrs[0] = &a;', explanation: 'We use the `&` address-of operator to find where `a` lives in memory, and store that address in the first slot of the array.' },
            { lineNumber: 14, code: '        printf("Value %d: %d\\n", i, *ptrs[i]);', explanation: 'This evaluates right-to-left. First, `ptrs[i]` fetches the memory address from the array. Then, the leading `*` dereferences that address to extract the integer.' },
          ],
          relatedTopicIds: ['u2-t3'],
        },
        {
          id: 'u2-t5-s1-ex2',
          title: 'Array of Strings (char pointers)',
          code: '#include <stdio.h>\n\nint main(void) {\n    /* Array of 3 string literal pointers */\n    const char *names[3] = {\n        "Alice",    /* 5 chars + \\0 */\n        "Bob",      /* 3 chars + \\0 */\n        "Charlie"   /* 7 chars + \\0 */\n    };\n\n    for (int i = 0; i < 3; i++) {\n        printf("Name %d: %s\\n", i, names[i]);\n    }\n    return 0;\n}',
          language: 'c',
          explanation: 'This is the canonical way to handle lists of text in C. When the compiler sees `"Alice"`, it stores those 6 bytes (\'A\',\'l\',\'i\',\'c\',\'e\',\'\\0\') in read-only memory and returns the starting memory address. The `names` array simply collects these three starting addresses. Notice how perfectly memory-efficient this is: "Bob" only takes 4 bytes of text memory, while "Charlie" takes 8 bytes. There is no wasted padding space, which is why this is preferred over 2D arrays for text.',
          expectedOutput: 'Name 0: Alice\nName 1: Bob\nName 2: Charlie',
          lineBreakdown: [
            { lineNumber: 5, code: '    const char *names[3] = {', explanation: 'Declares an array of 3 pointers. We use `const` because string literals are placed in read-only memory by the OS. Trying to modify them will cause a crash.' },
            { lineNumber: 12, code: '        printf("Name %d: %s\\n", i, names[i]);', explanation: '`names[i]` evaluates to a memory address pointing to the first character (\'A\', \'B\', or \'C\'). The `%s` specifier expects exactly this—a starting address—and prints characters until it hits `\\0`.' },
          ],
          relatedTopicIds: ['u2-t2'],
        },
      ],
      commonMistakes: [
        {
          id: 'u2-t5-s1-cm1',
          title: 'Forgetting to dereference',
          wrongCode: 'int *arr[2]; arr[0] = &x; \nprintf("%d", arr[0]);',
          correctCode: 'int *arr[2]; arr[0] = &x; \nprintf("%d", *arr[0]);',
          explanation: 'Because array syntax `arr[0]` usually gives us a value (like an int), it is easy to forget that in an array of pointers, `arr[0]` gives us a memory address. If you try to print it as an integer with `%d`, you are just printing the numerical value of the memory address (a huge, seemingly random number) rather than the data stored at that address.',
          consequence: 'Logical error. The program will print a garbage-looking number (the memory address) instead of the actual data, and modern compilers will generate a warning about mismatched printf format specifiers.',
        },
        {
          id: 'u2-t5-s1-cm2',
          title: 'Modifying string literals',
          wrongCode: 'char *names[1] = {"Bob"};\nnames[0][0] = \'M\'; /* Crash! */',
          correctCode: 'char names[1][10] = {"Bob"};\nnames[0][0] = \'M\';',
          explanation: 'This is a very common crash in C. When you write `char *names[1] = {"Bob"}`, the text "Bob" is baked directly into the executable file\'s read-only data section. The array `names` just holds a pointer to that read-only memory. When you attempt `names[0][0] = \'M\'`, the CPU triggers a hardware protection fault because you are trying to write to read-only memory. If you need to modify strings, you must allocate them in writable memory (like a standard 2D array `char names[1][10]`).',
          consequence: 'Immediate runtime crash (Segmentation Fault).',
        },
      ],
      interviewCallouts: [
        {
          id: 'u2-t5-s1-ic1',
          title: 'Array of Pointers vs 2D Array',
          content: 'This is a guaranteed interview question. A 2D array (`char arr[3][10]`) allocates a strict grid of 30 bytes in a single contiguous block; every string gets exactly 10 bytes, wasting space if the strings are short. An array of pointers (`char *arr[3]`) only allocates space for 3 memory addresses (24 bytes). Those addresses point to strings stored elsewhere in memory, which can be of any length. This creates a highly memory-efficient "jagged array".',
          relatedTopicIds: ['u2-t2'],
          frequency: 'common',
        },
      ],
      checkpoints: [
        {
          id: 'u2-t5-s1-cp1',
          title: 'Array of Pointers Mastery',
          description: 'Verify understanding of the memory model differences between 2D arrays and arrays of pointers.',
          criteria: [
            'Write the declaration for an array of 5 integer pointers',
            'Explain why an array of pointers is more memory-efficient for storing strings of varying lengths',
            'Explain the runtime danger of modifying string literals accessed via an array of pointers',
          ],
          topicId: 'u2-t5',
        },
      ],
      revisionCards: [
        {
          id: 'u2-t5-s1-rc1',
          front: 'What does the syntax `int *arr[5];` declare?',
          back: 'It declares an array named `arr` containing 5 elements. Because the brackets `[5]` bind tighter than the star `*`, each element is defined as a pointer to an integer (`int*`).',
          topicId: 'u2-t5',
          tags: ['arrays', 'pointers', 'declaration'],
        },
        {
          id: 'u2-t5-s1-rc2',
          front: 'Why is an array of pointers superior to a 2D array for storing a list of names?',
          back: 'A 2D array forces every row to be the exact same length (the length of the longest name), wasting memory with unused padding. An array of pointers allows "jagged arrays" where each string takes up only the exact memory it needs.',
          topicId: 'u2-t5',
          tags: ['arrays', 'strings', 'memory'],
        },
        {
          id: 'u2-t5-s1-rc3',
          front: 'Why should you always use `const char *` when creating an array of string literals?',
          back: 'Because string literals are mapped into read-only memory by the operating system. If you omit `const`, the compiler lets you write code that attempts to modify them, which will result in a violent runtime crash (Segmentation Fault). `const` catches this error at compile time.',
          topicId: 'u2-t5',
          tags: ['strings', 'const', 'pointers'],
        },
      ],
    },
    {
      id: 'u2-t5-s2',
      title: 'Command Line Arguments (argv)',
      slug: 'command-line-arguments',
      description: 'When you run a C program from the terminal, you often want to pass it input data immediately (e.g., `./gcc main.c`). How does the C program receive this text? Through an array of pointers called `argv` (argument vector). The operating system parses the terminal command, allocates memory for each word as a string, and hands your `main` function an array of pointers pointing to those strings.',
      keyPoints: [
        'To accept command line arguments, you change your main signature to: `int main(int argc, char *argv[])`.',
        '`argc` (Argument Count) is an integer indicating exactly how many words were typed in the terminal command.',
        '`argv` (Argument Vector) is an array of character pointers. Each pointer points to one of the words from the command line.',
        '`argv[0]` is always a pointer to the name of the executable itself (e.g., `./program`).',
        'The actual data you care about starts at `argv[1]` and goes up to `argv[argc - 1]`.',
        'The C standard guarantees that `argv[argc]` will always be a `NULL` pointer, marking the definitive end of the array.',
      ],
      codeExamples: [
        {
          id: 'u2-t5-s2-ex1',
          title: 'Printing Command Line Arguments',
          code: '#include <stdio.h>\n\n/* Run with: ./program arg1 arg2 */\nint main(int argc, char *argv[]) {\n    printf("Total arguments: %d\\n", argc);\n    \n    for (int i = 0; i < argc; i++) {\n        printf("argv[%d]: %s\\n", i, argv[i]);\n    }\n    return 0;\n}',
          language: 'c',
          explanation: 'This code reveals how C programs interface with the outside world. When the user types `./program arg1 arg2`, the OS builds three strings in memory. It sets `argc` to 3, and populates the `argv` array with three pointers. The `for` loop simply walks down this array of pointers, using `%s` to print the string that each pointer leads to.',
          expectedOutput: 'Total arguments: 3\nargv[0]: ./program\nargv[1]: arg1\nargv[2]: arg2',
          lineBreakdown: [
            { lineNumber: 4, code: 'int main(int argc, char *argv[]) {', explanation: 'The standard signature for a program that takes inputs. The OS provides the values for `argc` and `argv` when it launches the executable.' },
            { lineNumber: 7, code: '    for (int i = 0; i < argc; i++) {', explanation: 'We strictly loop from `0` to `argc - 1`. `argc` is the exact number of valid pointers in the array.' },
            { lineNumber: 8, code: '        printf("argv[%d]: %s\\n", i, argv[i]);', explanation: '`argv[i]` extracts a pointer from the array. `%s` takes that pointer and prints the characters it points to.' },
          ],
          relatedTopicIds: [],
        },
      ],
      commonMistakes: [
        {
          id: 'u2-t5-s2-cm1',
          title: 'Using arguments without checking argc',
          wrongCode: 'int main(int argc, char *argv[]) {\n    printf("Hello %s", argv[1]);\n}',
          correctCode: 'int main(int argc, char *argv[]) {\n    if (argc < 2) return 1;\n    printf("Hello %s", argv[1]);\n}',
          explanation: 'If a user simply types `./program` and hits enter, `argc` is exactly 1. The only valid pointer is `argv[0]` (the program name). If your code blindly assumes `argv[1]` exists and tries to access it, you will fetch a `NULL` pointer (because `argv[argc]` is guaranteed to be `NULL`). Feeding a `NULL` pointer into `printf("%s")` will cause an immediate crash.',
          consequence: 'Segmentation fault as soon as the user runs the program without providing the expected arguments.',
        },
      ],
      interviewCallouts: [
        {
          id: 'u2-t5-s2-ic1',
          title: 'What does `char **argv` mean?',
          content: 'You will frequently see the signature written as `int main(int argc, char **argv)`. In C, when an array is passed as a function parameter, it automatically decays into a pointer to its first element. Since `argv` is an array of `char *`, a pointer to its first element is a pointer to a `char *`. Therefore, the array of pointers decays into a double pointer (`char **`). Both signatures are exactly identical in functionality, but `char *argv[]` is slightly more readable for beginners.',
          relatedTopicIds: ['u2-t4'],
          frequency: 'occasional',
        },
      ],
      checkpoints: [
        {
          id: 'u2-t5-s2-cp1',
          title: 'Command Line Args',
          description: 'Verify understanding of the command line argument memory model.',
          criteria: [
            'Explain what `argc` represents and how it is determined',
            'Explain what data is strictly guaranteed to be located at `argv[0]`',
            'Write defensive code that prevents crashing if a user omits expected arguments',
          ],
          topicId: 'u2-t5',
        },
      ],
      revisionCards: [
        {
          id: 'u2-t5-s2-rc1',
          front: 'In `int main(int argc, char *argv[])`, what exactly is `argc`?',
          back: '`argc` stands for "Argument Count". It is an integer representing the total number of words typed in the terminal command, including the name of the executable itself.',
          topicId: 'u2-t5',
          tags: ['argv', 'main'],
        },
        {
          id: 'u2-t5-s2-rc2',
          front: 'What data is always stored at `argv[0]`?',
          back: 'A pointer to a string containing the name (and sometimes the path) of the program currently being executed (e.g., `"./a.out"` or `"./program"`).',
          topicId: 'u2-t5',
          tags: ['argv', 'main'],
        },
      ],
    },
  ],

  theoryQuestions: [
    {
      id: 'u2-t5-q1',
      type: 'mcq',
      topicId: 'u2-t5',
      difficulty: 'beginner',
      question: 'Which is the correct declaration for an array of 10 character pointers?',
      options: ['char (*arr)[10];', 'char *arr[10];', 'char arr*[10];', 'char arr[10]*;'],
      correctAnswer: 'char *arr[10];',
      explanation: 'In C syntax declarations, the bracket operator `[]` binds tighter than the star operator `*`. Thus, `char *arr[10]` is read as "an array `[10]` containing pointers `*` to characters `char`". Writing `char (*arr)[10]` would force the star to bind first, creating a single pointer to an entire 10-character array (a very different construct!).',
      tags: ['arrays', 'pointers', 'declaration'],
    },
    {
      id: 'u2-t5-q2',
      type: 'predict-output',
      topicId: 'u2-t5',
      difficulty: 'intermediate',
      question: 'What is the output?',
      code: '#include <stdio.h>\nint main(void) {\n    int a = 1, b = 2;\n    int *arr[2] = {&a, &b};\n    *arr[0] = 5;\n    printf("%d", a);\n    return 0;\n}',
      correctAnswer: '5',
      explanation: 'The element `arr[0]` holds the memory address of `a`. By using the dereference operator `*` on it (`*arr[0] = 5`), we are telling the CPU to go to that memory address and overwrite the data with the number 5. This indirectly modifies the variable `a`.',
      tags: ['arrays', 'pointers', 'dereference'],
    },
    {
      id: 'u2-t5-q3',
      type: 'true-false',
      topicId: 'u2-t5',
      difficulty: 'beginner',
      question: 'In `int main(int argc, char *argv[])`, if the user runs `./app hello`, `argc` will be 1.',
      correctAnswer: false,
      explanation: 'The argument count includes the name of the program itself. So `argv[0]` points to "./app", and `argv[1]` points to "hello". Therefore, `argc` will be exactly 2.',
      tags: ['argv', 'main'],
    },
    {
      id: 'u2-t5-q4',
      type: 'spot-bug',
      topicId: 'u2-t5',
      difficulty: 'intermediate',
      question: 'Spot the bug:',
      code: 'char *days[2] = {"Sat", "Sun"};\nprintf("%c", days[1]);',
      correctAnswer: 'Trying to print a string pointer with %c format specifier.',
      explanation: 'The element `days[1]` is a `char *` (a pointer to the letter \'S\' in "Sun"). To print an entire string from a pointer, you must use the `%s` format specifier. Using `%c` attempts to print a single character, but you are passing it a memory address, which will result in garbage output or a crash.',
      tags: ['printf', 'arrays', 'strings'],
    },
    {
      id: 'u2-t5-q5',
      type: 'mcq',
      topicId: 'u2-t5',
      difficulty: 'advanced',
      question: 'How do you access the second character of the first string in an array of strings: char *arr[3]; ?',
      options: ['arr[0][1]', 'arr[1][0]', '*arr[0] + 1', 'arr(0,1)'],
      correctAnswer: 'arr[0][1]',
      explanation: 'First, `arr[0]` dives into the array to retrieve the first pointer (the address of the first string). Because strings are just contiguous characters in memory, we can use a second set of brackets `[1]` on that resulting pointer to step forward by one character. Thus, `arr[0][1]` correctly retrieves the second character.',
      tags: ['arrays', 'pointers', 'syntax'],
    },
  ],

  programmingProblems: [
    {
      id: 'u2-t5-new-easy',
      title: 'Array Reverse',
      topicId: 'u2-t5',
      difficulty: 'beginner',
      problemStatement: 'Reverse an array of N elements in place.',
      constraints: ['Do not use a secondary array'],
      sampleInput: '1 2 3',
      sampleOutput: '3 2 1',
      hints: ['Swap elements from both ends moving towards the center'],
      solution: '/* Array reverse implementation */',
      solutionExplanation: 'Swaps index i and N-1-i.',
      dryRun: [],
      tags: ['arrays']
    },
    {
      id: 'u2-t5-new-med',
      title: 'Matrix Diagonal Sum',
      topicId: 'u2-t5',
      difficulty: 'intermediate',
      problemStatement: 'Calculate the sum of the main diagonal of an NxN matrix.',
      constraints: ['Matrix is guaranteed to be square'],
      sampleInput: '1 2\n3 4',
      sampleOutput: '5',
      hints: ['Main diagonal elements have index [i][i]'],
      solution: '/* Diagonal sum implementation */',
      solutionExplanation: 'Iterates and sums arr[i][i].',
      dryRun: [],
      tags: ['matrix']
    },
    {
      id: 'u2-t5-new-hard',
      title: 'Recursive GCD',
      topicId: 'u2-t5',
      difficulty: 'advanced',
      problemStatement: 'Find the Greatest Common Divisor of two numbers using recursion (Euclidean algorithm).',
      constraints: ['Must use recursion'],
      sampleInput: '48 18',
      sampleOutput: '6',
      hints: ['gcd(a, b) = gcd(b, a % b)'],
      solution: '/* Recursive GCD implementation */',
      solutionExplanation: 'Implements Euclidean algorithm recursively.',
      dryRun: [],
      tags: ['recursion']
    },
    {
      id: 'u2-t5-p1',
      title: 'Dictionary Lookup',
      topicId: 'u2-t5',
      difficulty: 'intermediate',
      problemStatement: 'Write a program with an array of strings representing days of the week ("Monday", "Tuesday", etc.). Take an integer 1-7 from the user and print the corresponding day. Handle invalid inputs.',
      constraints: ['Use an array of char pointers', '1 = Monday, 7 = Sunday'],
      sampleInput: '3',
      sampleOutput: 'Wednesday',
      hints: ['const char *days[7] = {"Monday", ...};', 'Remember array indices are 0-6, so subtract 1 from the input.'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    const char *days[7] = {\n        "Monday", "Tuesday", "Wednesday", \n        "Thursday", "Friday", "Saturday", "Sunday"\n    };\n    \n    int dayNum;\n    printf("Enter day (1-7): ");\n    scanf("%d", &dayNum);\n    \n    if (dayNum >= 1 && dayNum <= 7) {\n        printf("%s\\n", days[dayNum - 1]);\n    } else {\n        printf("Invalid day\\n");\n    }\n    return 0;\n}',
      solutionExplanation: 'An array of strings is the perfect mapping data structure. By storing the days in order, their physical array index (0-6) naturally correlates to the day number (1-7). We simply subtract 1 from the user\'s input to align it with the zero-indexed array, immediately retrieving the correct string pointer without needing a massive `if-else` or `switch` block.',
      dryRun: [
        { step: 1, line: 10, variables: { dayNum: '3' }, output: '', explanation: 'User inputs the integer 3.' },
        { step: 2, line: 12, variables: {}, output: '', explanation: 'The conditional checks if 3 is within the valid 1-7 range.' },
        { step: 3, line: 13, variables: {}, output: 'Wednesday\\n', explanation: 'Evaluates `days[3 - 1]`, which is `days[2]`. It fetches the pointer at index 2 and `printf` prints the text "Wednesday".' },
      ],
      tags: ['arrays', 'strings', 'mapping'],
    },
    {
      id: 'u2-t5-p2',
      title: 'Pointer Array Sum',
      topicId: 'u2-t5',
      difficulty: 'beginner',
      problemStatement: 'Create 3 integer variables. Create an array of 3 pointers pointing to these variables. Write a loop that uses the array of pointers to sum the values of the 3 variables.',
      constraints: ['Must use a loop', 'Must dereference the pointers in the array'],
      sampleInput: '',
      sampleOutput: 'Sum: 60',
      hints: ['Initialize: ptrs[0] = &a;', 'In the loop: sum += *ptrs[i];'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int a = 10, b = 20, c = 30;\n    int *ptrs[3];\n    \n    ptrs[0] = &a;\n    ptrs[1] = &b;\n    ptrs[2] = &c;\n    \n    int sum = 0;\n    for(int i = 0; i < 3; i++) {\n        sum += *ptrs[i];\n    }\n    \n    printf("Sum: %d\\n", sum);\n    return 0;\n}',
      solutionExplanation: 'This demonstrates the mechanics of indirection. The array `ptrs` does not contain any of the numbers we are summing. Instead, the loop walks through the array, pulling out memory addresses one by one. The `*` operator then follows those addresses to fetch the actual values from variables `a`, `b`, and `c`, accumulating them into `sum`.',
      dryRun: [
        { step: 1, line: 9, variables: { a: '10', b: '20', c: '30' }, output: '', explanation: 'The array is populated with the memory addresses of the three distinct variables.' },
        { step: 2, line: 13, variables: { i: '0', sum: '10' }, output: '', explanation: '`ptrs[0]` yields the address of `a`. `*` dereferences it to get 10. `sum` becomes 10.' },
        { step: 3, line: 13, variables: { i: '1', sum: '30' }, output: '', explanation: '`ptrs[1]` yields the address of `b`. `*` dereferences it to get 20. `sum` becomes 30.' },
        { step: 4, line: 13, variables: { i: '2', sum: '60' }, output: '', explanation: '`ptrs[2]` yields the address of `c`. `*` dereferences it to get 30. `sum` becomes 60.' },
      ],
      tags: ['pointers', 'arrays', 'dereference'],
    },
    {
      id: 'u2-t5-p3',
      title: 'Echo Command Line Arguments',
      topicId: 'u2-t5',
      difficulty: 'intermediate',
      problemStatement: 'Write a program that prints all its command line arguments separated by spaces, skipping the program name (argv[0]). If no extra arguments are given, print "No arguments".',
      constraints: ['Use argc and argv', 'Output must be on a single line'],
      sampleInput: './program hello world',
      sampleOutput: 'hello world ',
      hints: ['If argc == 1, print "No arguments".', 'Loop from i = 1 to argc - 1.'],
      solution: '#include <stdio.h>\n\nint main(int argc, char *argv[]) {\n    if (argc == 1) {\n        printf("No arguments\\n");\n        return 0;\n    }\n    \n    for (int i = 1; i < argc; i++) {\n        printf("%s ", argv[i]);\n    }\n    printf("\\n");\n    \n    return 0;\n}',
      solutionExplanation: 'This is the standard pattern for processing command line input. By intentionally starting our loop at `i = 1`, we skip `argv[0]` (which contains the executable name) and only process the user\'s actual arguments. The initial `argc == 1` check is crucial defensive programming to gracefully handle the case where the user provided no extra arguments.',
      dryRun: [
        { step: 1, line: 4, variables: { argc: '3' }, output: '', explanation: 'Assuming the user runs `./program hello world`, the OS sets `argc` to 3.' },
        { step: 2, line: 9, variables: { i: '1' }, output: 'hello ', explanation: 'The loop starts at index 1. `argv[1]` points to the string "hello", which is printed.' },
        { step: 3, line: 9, variables: { i: '2' }, output: 'world ', explanation: 'The loop advances to index 2. `argv[2]` points to the string "world", which is printed.' },
      ],
      tags: ['argv', 'command-line', 'strings'],
    },
  ],
};
